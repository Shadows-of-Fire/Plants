package shadows.plants.common;

public enum EnumAdvTempZone {

	MOUNTAIN(0.0f, 0.0f, 0d, 0d, 0.0f, 0.0f);

	EnumAdvTempZone(float minTemp, float maxTemp, double maxY, double minY, float minHum, float maxHum) {
	}
}

// Not yet implemented. For future if I ever move away from EnumTempZone to
// allow for even greater decoration control.